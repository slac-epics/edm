# CONFIG_SITE,v 1.2 2007/07/03 21:37:52 jba Exp

# Make any extensions-specific changes to the EPICS build
# configuration variables in this file.

# Extensions should not normally be built for cross targets
CROSS_COMPILER_TARGET_ARCHS =

# Except for SLAC PCDS, where we use linux-x86_64 to
# build both 32 bit and 64 bit executables
BUILD_FOR_HOST_ARCH=1
ifeq '$(EPICS_HOST_ARCH)' 'linux-x86_64'
CROSS_COMPILER_TARGET_ARCHS +=  linux-x86
endif
#ifeq '$(EPICS_HOST_ARCH)' 'rhel6-x86_64'
#CROSS_COMPILER_TARGET_ARCHS +=  rhel6-x86_64-debug
#endif

# If you don't want to install into $(TOP) then
# define INSTALL_LOCATION here
#INSTALL_LOCATION=<fullpathname>

# To create a shared library set SHARED_LIBRARIES to YES in Makefile
SHARED_LIBRARIES = YES
STATIC_BUILD = NO

USR_CXXFLAGS += -Wno-write-strings
USR_CXXFLAGS += -g
USR_CXXFLAGS += -O0
USR_CFLAGS += -std=gnu99
